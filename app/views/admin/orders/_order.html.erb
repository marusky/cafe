<div class='bg-white shadow-lg shadow-zinc-200 border border-zinc-200 rounded-xl p-4'>
  <div class='flex justify-between items-center'>
    <h3 class='text-2xl font-medium'>#<%= order.id %></h3>
    <div class='px-3 py-1 rounded-2xl bg-orange-500 text-orange-50'>
      <div class=''>
        Došlá
      </div>
    </div>
  </div>
  <div class='text-zinc-600 mt-4'>
    <ul class='list-disc pl-4'>
      <% order.order_items.each do |order_item| %>
        <li><%= order_item.amount %>x <%= order_item.product.title %></li>
      <% end %>
    </ul>
  </div>
  <div class='mt-4 flex justify-between items-center gap-4'>
    <%= button_to 'Odmietnuť', cancel_order_path(order), method: :patch, form_class: 'w-full py-3 bg-rose-500 hover:bg-rose-300 text-rose-50 rounded-xl font-medium text-lg text-center' %>
    <%= button_to 'Prijať', receive_order_path(order), method: :patch, form_class: 'w-full py-3 bg-green-400 hover:bg-green-300 text-green-50 rounded-xl font-medium text-lg text-center' %>
  </div>
</div>