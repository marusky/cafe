<% content_for :title, "Objednávka ##{@order.id}" %>
<% content_for :back do %>
  <%= link_to params[:back_path] || app_path, class: 'text-sky-500' do %>
    &lt; Späť
  <% end %>
<% end %>

<%= turbo_stream_from @order %>

<div data-controller="visibility-refresh">
  <%= render "orders/states/#{@order.state}", order: @order %>
</div>