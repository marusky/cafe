<header class='z-20 fixed top-0 left-0 right-0 bg-zinc-50 h-16 px-5 shadow-lg shadow-zinc-400/10'>
  <div class='flex justify-between items-center h-16'>
    <div>
      <%= yield :back %>
    </div>
    <h1 class='absolute left-1/2 -translate-x-1/2 font-semibold text-xl'>
      <%= yield :title %>
    </h1>
    <div>
      <%= yield :action %>
    </div>
  </div>
</header>