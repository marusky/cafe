Ako ťa máme volať?

<%= form_with model: @customer do |form| %>
  <div class="my-5">
    <%= form.label :name %>
    <%= form.text_field :name, class: "block shadow rounded-md border border-gray-400 outline-none px-3 py-2 mt-2 w-full" %>
    <% if @customer.errors.any? %>
      meno poprosim 
    <% end %>
  </div>
  <%= form.submit 'Tak ma volajte' %>
<% end %>
